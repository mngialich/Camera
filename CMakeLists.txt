CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(TEGRACAMERA)

FIND_PACKAGE(OpenCV REQUIRED)

# Add CUDA Package

FIND_PACKAGE(CUDA REQUIRED)

INCLUDE_DIRECTORIES(
  ${CUDE_CUT_INCLUDE_DIR})

CUDA_ADD_EXECUTABLE(Camera
  Main.cpp
  Source/VCamera.cpp)
	
TARGET_LINK_LIBRARIES(Camera
  ${OpenCV_LIBS} )
